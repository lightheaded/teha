apiVersion: v1
kind: Service
metadata:
  name: teha
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: teha
    app.kubernetes.io/component: server
  ports:
    - name: http
      port: 8637
      targetPort: http
      protocol: TCP
